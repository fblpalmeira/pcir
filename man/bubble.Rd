% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bubble.R, R/pcir.package.R
\name{bubble}
\alias{bubble}
\title{Create a Bubble Plot for PCI Visualization}
\usage{
bubble(
  df3,
  scale_type = c("bipolar", "unipolar"),
  ylim_range = 4,
  unipolar_ylim = c(1, 9),
  xlab = "",
  ylab = "Action acceptability",
  title = NULL,
  bubble_color = "gray80",
  bubble_stroke = "black"
)

bubble(
  df3,
  scale_type = c("bipolar", "unipolar"),
  ylim_range = 4,
  unipolar_ylim = c(1, 9),
  xlab = "",
  ylab = "Action acceptability",
  title = NULL,
  bubble_color = "gray80",
  bubble_stroke = "black"
)
}
\arguments{
\item{df3}{A data frame generated by the \code{pci} function, containing the PCI values and other statistics.}

\item{scale_type}{The scale type used for the plot: 'bipolar' or 'unipolar'. Default is 'bipolar'.}

\item{ylim_range}{For 'bipolar' scale, the range of the y-axis. Must be an integer between 1 and 4.}

\item{unipolar_ylim}{A numeric vector of length 2 specifying the y-axis limits for 'unipolar' scale.}

\item{xlab}{The label for the x-axis. Default is an empty string.}

\item{ylab}{The label for the y-axis. Default is 'Action acceptability'.}

\item{title}{Optional title for the plot. Default is NULL.}

\item{bubble_color}{The fill color of the bubbles. Default is 'gray80'.}

\item{bubble_stroke}{The border color of the bubbles. Default is 'black'.}
}
\value{
A ggplot2 object representing the bubble plot.

A ggplot2 object representing the bubble plot.
}
\description{
This function generates a bubble plot to visualize the results of the PCI
calculation. It shows the mean action acceptability on the y-axis and the PCI
value as the size of the bubbles.

Create a bubble plot to visualize PCI results.
This function generates a bubble plot where each bubble represents a group.
The size of the bubble corresponds to the PCI value, and the y-axis shows the mean
action acceptability for each group.
}
\details{
The bubble plot displays the 'Mean' value on the y-axis and the PCI value as the size of the bubbles.
It helps to visualize the relationship between action acceptability (mean score) and the potential
for conflict index (PCI) for each category in the dataset.
}
\examples{
data <- data.frame(
  Category = rep(c('A', 'B', 'C'), each = 3),
  Value = c(-1, 0, 1, -1, 0, 1, -1, 0, 1)
)
df2 <- counting(data)
df3 <- pci(df2)
bubble(df3)

df3 <- pci(df2)
bubble(df3)
}
